/*
  Background line effect removed (mesh + sketch thread both pulled).
  Stub kept so existing <NetSegment name="..."/> call sites across the
  homepage don't need touching — it just renders nothing.
*/
export const NetSegment = () => null;

export default NetSegment;
